# Dependensi khusus skrip build `python -m bangun` — TIDAK dipakai runtime,
# dan sengaja tidak masuk image Docker (image memakai data-salinan.tar.gz
# yang sudah jadi). Dipisah karena keempatnya besar: memasangnya di image
# menambah ratusan MB tanpa satu pun baris src/ yang mengimpornya.
#
# `bangun/kembar.py` memakai numpy/pandas/scikit-learn, `bangun/geo.py`
# memakai shapely.
numpy==2.4.6
pandas==3.0.5
scikit-learn==1.9.0
shapely==2.1.2
